     <FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/colorAccent"
    tools:context=".activities.reminder.Reminder">

    <!-- The primary full-screen view. This can be replaced with whatever view
         is needed to present your content, e.g. VideoView, SurfaceView,
         TextureView, etc. -->


    <!-- This FrameLayout insets its children based on system windows using
         android:fitsSystemWindows. -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:fitsSystemWindows="true"
        android:orientation="vertical"
        >
        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_margin="10dp"
            android:fontFamily="sans-serif-thin"
            android:gravity="center"
            android:text="@string/duty_header"
            android:textColor="@color/colorPrimary"
            android:textSize="50dp" />

        <View android:id="@+id/duty_line"
            android:layout_width="match_parent"
            android:layout_height="1dp"
            android:background="@color/black_overlay" />

        <LinearLayout
            android:id="@+id/duty_dutycontainer"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_margin="10dp"
            android:orientation="horizontal">

            <TextView
                android:id="@+id/duty_dutyname"
                android:layout_width="50dp"
                android:layout_height="wrap_content"
                android:layout_margin="10dp"
                android:layout_gravity="center_vertical"
                android:layout_weight=".7"
                android:gravity="center"
                android:fontFamily="sans-serif"
                android:text="@string/trash"
                android:textColor="@color/black"
                android:textSize="35dp" />

            <!-- Delete text="Amsterdam" here and use textview.settext(random_user_name)
            from a repeating cycle (Ask Zach about this)in .java file

             ID= duty_assigneduser -->
            <TextView
                android:id="@+id/duty_assigneduser"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_weight=".3"
                android:gravity="center"
                android:layout_gravity="center_vertical"
                android:text="Amsterdam"
                android:textColor="@color/black"
                android:textSize="40dp"
                android:fontFamily="sans-serif-thin"/>

        </LinearLayout>

        <View
            android:layout_width="match_parent"
            android:layout_height="1dp"
            android:background="@color/black_overlay" />



        <View
            android:layout_width="match_parent"
            android:layout_height="1dp"
            android:background="@color/black_overlay" />




        <ListView
            android:id="@android:id/list"
            android:layout_width="fill_parent"
            android:layout_height="fill_parent"
            android:drawSelectorOnTop="false"
            />
    </LinearLayout>

    <!-- + button to create duties.
        ID = duty_addbtn -->
    <Button
        android:id="@+id/duty_addbtn"
        android:layout_width="70dp"
        android:layout_height="70dp"
        android:layout_gravity="end|bottom"
        android:layout_margin="10dp"
        android:background="@drawable/circleblue"
        android:text="@string/plus"
        android:textColor="@color/colorAccent"
        android:textSize="40dp" />
</FrameLayout>
